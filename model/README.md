# Model artifacts

The modeling notebook exports the trained XGBoost model and the selected feature list to this directory:

```text
model/xgb_churn_model.pkl
model/feature_names.json
```

Generated model artifacts are intentionally ignored by Git so the repository stays lightweight and reproducible from source.
